{{ component('public_contact_list', {
    search: search ?? '',
    categoryId: categoryId ?? null,
    radiusKm: radiusKm ?? null,
    latitude: latitude ?? null,
    longitude: longitude ?? null,
    viewMode: viewMode ?? 'grid',
    limit: limit ?? 20
}) }}
